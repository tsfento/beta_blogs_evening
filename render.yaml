databases:
  - name: beta_blogs_evening
    databaseName: beta_blogs_evening
    user: beta_blogs_evening
    plan: free

services:
  - type: web
    name: beta_blogs_evening
    runtime: ruby
    plan: free
    buildCommand: './bin/render-build.sh'
    startCommand: './bin/rails server'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: beta_blogs_evening
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2
